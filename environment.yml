name: canary-change-probe
channels:
  - conda-forge
dependencies:
  - python=3.10
  - numpy>=1.24
  - scipy>=1.10
  - scikit-learn>=1.2
  - pillow>=9.0
  - pyyaml>=6.0
  - pytest>=7.0
  - pip
  - pip:
      - torch>=2.0
      - torchvision>=0.15
